#服务端口号
server:
  port: 9090
#Data source config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/toolsplatform?characterEncoding=UTF-8&useSSL=true
    username: root
    password: 672025
mybatis-plus:
  mapper-locations: com/lyentech/toolsplatform/*/*Mapper.xml
  #日志文件的配置
logging:
  level:
    root: info
